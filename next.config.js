module.exports = {
  reactStrictMode: true,
  // NFT disables eslint to prevent errors from being thrown during build.
  // Feel free to remove this if you prefer to be bothered by eslint.
  eslint: {
    ignoreDuringBuilds: true
  }
};
